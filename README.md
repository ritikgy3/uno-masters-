# UNO Masters

Online real-time multiplayer card game for 2–6 players or exactly 8 players. A room with exactly 7 players cannot start.

## Run
npm install
npm start

## Deploy
Deploy as a Node.js Web Service with build command `npm install` and start command `npm start`. WebSockets must be supported.
